import Topology from './Topology';
import style from '../../styles/hero.module.css';

const Hero = function () {
  return (
    <section className={style.hero}>
      <Topology />
    </section>
  );
};

export default Hero;
