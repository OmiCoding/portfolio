import Topology from './Topology';

import '../../styles/hero.css';

const Hero = function () {
  return (
    <section className="hero">
      <Topology />
    </section>
  );
};

export default Hero;
